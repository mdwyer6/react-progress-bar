import ReactDOM from 'react-dom';
import ProgressBar from './components/progressbar.jsx';

export default {
  ProgressBar: {
    render: () => {
      ReactDOM.render(
        <ProgressBar />
      );
    }
  }
}